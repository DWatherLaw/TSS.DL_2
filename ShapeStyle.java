/**
 * Перерахування, що визначає стиль фігури.
 */
public enum ShapeStyle {
    DEFAULT_THICKNESS, // Стандартна товщина (case 3)
    THICK_3PX,         // Товщина 3 пікселі (case 1)
    THICK_7PX,         // Товщина 7 пікселів (case 4)
    WHITE_GRAY_GRADIENT, // Градієнт (case 7)
    RED_COLOR;          // Червоний колір (case 8)
}